# raghavmirchandani.github.io

Personal site, served at https://raghavmirchandani.github.io/

Plain static HTML — no build step. Edit `index.html`, commit, push; GitHub Pages redeploys automatically.

`.nojekyll` is present so GitHub serves the files as-is instead of running them through Jekyll.
